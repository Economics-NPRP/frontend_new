'use server';

// Deprecated: authedFetch is no longer used. All server-side calls go through the same-origin proxy
// via absolute URLs from internalUrl(). This file is intentionally left blank to avoid imports.

export {};
