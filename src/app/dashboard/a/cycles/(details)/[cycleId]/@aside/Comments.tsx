import { Stack } from '@mantine/core';

import classes from './styles.module.css';

export const CommentsContent = () => {
	return <Stack className={`${classes.comments} ${classes.content}`}>Comments Content</Stack>;
};
