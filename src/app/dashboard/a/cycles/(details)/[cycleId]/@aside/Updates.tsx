import { Stack } from '@mantine/core';

import classes from './styles.module.css';

export const UpdatesContent = () => {
	return <Stack className={`${classes.updates} ${classes.root}`}>Updates Content</Stack>;
};
